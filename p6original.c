#include<stdio.h>
#include<string.h>
void input_string(char a)
{
  printf("enter the string to be revered\n");
  scanf("%s",&a);
}
char str_reverse()
{
  char a[60];
  int g,leng;
  
  for(g = leng - 1; g >= 0; g--) {
        printf("%c", reverse[]);
    }
}
void output(char a,char *reversea)